{
  "name": "anish-some-package",
  "version": "1.0.3",
  "scripts": {
    "release": "git push --dry-run && standard-version && git push --follow-tags origin master"
  },
  "devDependencies": {
    "standard-version": "^9.0.0"
  }
}
